# Task 001: プロジェクト初期セットアップ

## 概要
privacy-incident-catalog の初期プロジェクトをセットアップし、開発基盤を構築する。

## 完了条件
- [ ] npm install が成功する
- [ ] npm run dev で開発サーバーが起動する
- [ ] npm run build でビルドが成功する
- [ ] npm run test でテストが通る
- [ ] npm run validate でバリデーションが通る
- [ ] 空の一覧ページが表示される

## 作業内容
1. package.json の依存関係を確認し、npm install を実行
2. src/ 配下の主要ファイル（App.tsx, main.tsx, Context等）を動作するように接続
3. 空のpublic/cases/index.json を作成
4. 初回ビルド・テストを実行
